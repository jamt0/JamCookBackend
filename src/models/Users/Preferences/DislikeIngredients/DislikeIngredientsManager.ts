import { Request } from "express";

export default class DislikeIngredientsManager {
  public static getDislikeIngredients = async (req: Request) => {}

  public static getDislikeIngredientsUser = async (req: Request) => {}
}